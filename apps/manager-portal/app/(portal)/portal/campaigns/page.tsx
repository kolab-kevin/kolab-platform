import { PlaceholderWorkspace } from '@/components/common/placeholder-workspace';

export default function CampaignsPage() {
  return (
    <PlaceholderWorkspace
      title="Campaigns"
      description="Campaign pipeline and deliverable oversight"
      moduleTitle="Campaign operations"
      moduleDescription="MP-04 will add campaign command views, assignments, and deliverable tracking."
    />
  );
}
