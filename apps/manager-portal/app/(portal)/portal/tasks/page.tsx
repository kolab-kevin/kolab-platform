import { PlaceholderWorkspace } from '@/components/common/placeholder-workspace';

export default function TasksPage() {
  return (
    <PlaceholderWorkspace
      title="Tasks"
      description="Operational tasks and alerts"
      moduleTitle="Notifications and tasks"
      moduleDescription="MP-06 will add task queues, alert routing, and manager notifications."
    />
  );
}
